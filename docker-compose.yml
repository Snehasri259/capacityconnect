services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: capacity_connect
      POSTGRES_USER: capacity
      POSTGRES_PASSWORD: capacity_dev
    ports:
      - "5432:5432"
    volumes:
      - capacity_pg:/var/lib/postgresql/data
volumes:
  capacity_pg:
